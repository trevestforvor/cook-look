name: Deploy editor to GitHub Pages

# Builds a static export of the Chroma editor and publishes it to GitHub Pages.
# The AI assistant is disabled in this build (it needs a server); every other
# feature runs fully client-side.
#
# The Configure Pages step auto-enables Pages (Source = GitHub Actions) on the
# first run, so no manual repo setting is required. The site is served at
# https://<owner>.github.io/cook-look/

on:
  push:
    branches: [main]
  workflow_dispatch:

# Allow the workflow to publish to Pages via OIDC.
permissions:
  contents: read
  pages: write
  id-token: write

# Only one concurrent deploy; don't cancel an in-progress one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Enables Pages with Source = "GitHub Actions" if it isn't already, so the
      # deploy step's first run doesn't 404 on a not-yet-enabled Pages site.
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Build static export
        run: pnpm build:static

      - name: Disable Jekyll (preserve _next assets)
        run: touch apps/web/out/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
